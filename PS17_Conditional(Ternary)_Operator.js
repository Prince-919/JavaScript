
/********* 2️⃣Conditional (ternary) Operator ******/
// The conditional (ternary ) operator is the only JavaScript Operator that takes three operands

// Syntax 
// variableName = (condition) ? value1 : value2

// method if else
var age = 19;
if(age >= 18)
{
    console.log("you can vate");
}else{
    console.log("you con't vote");
}

// method ternary 

var age =19;
console.log((age = 18) ? "you can vote" : "you con't vote");